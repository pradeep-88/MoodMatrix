# Emotion backend API base URL (no trailing slash).
# Must be running for webcam frame-detect to work.
# Example: cd emotion_ui_backend && source .venv/bin/activate && uvicorn app.main:app --reload --port 8000
VITE_API_URL=http://127.0.0.1:8000
